RUN-1 — A first task, so you can see the board work end to end.

Replace this with a real brief. What a brief is for: it is the whole instruction the model gets, and the
board also reads it to know which files this task may touch, so two tasks that edit the same files queue
instead of running at once.

## What to do

Write a file `hello.txt` at the project root containing one line: the current date.

## Files you may edit

- `hello.txt`

## Delivery

Say in one line what you wrote.
