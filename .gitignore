node_modules/
dist/
target/
src-tauri/target/
desktop/src-tauri/gen/schemas/
.env
.env.*
*.log
.questboard-data/
.claude/
CLAUDE.local.md
# migration staging copy of the first version (holds project data); never commit
_import/
# machine-local project configs and notes (e.g. configs pointing at private game repos); never commit
local/

# executor scratch state must never be committed
.omo/
.codex/

# agent-teams runtime state
.agent-teams/
